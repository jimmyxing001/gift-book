# 电子礼簿系统 — 项目结构分析

> 分析日期：2026-08-16  
> 仓库来源：https://github.com/jimmyxing001/gift-book  
> 本地路径：`D:\WorkBuddy\gift-book`

## 一、项目类型与技术栈

这是一个**纯前端、零后端、离线可运行**的单页 Web 应用。

| 层级 | 技术 |
|------|------|
| 核心框架 | Vanilla JS（ES6+ 模块），面向对象架构 |
| 样式 | Tailwind CSS（本地 vendored，未使用 CDN） |
| 数据存储 | IndexedDB（浏览器本地） |
| 数据加密 | CryptoJS（AES-256 加密数据 + SHA-256 哈希密码） |
| 表格组件 | Grid.js |
| Excel 导出 | SheetJS（xlsx.full.min.js） |
| PDF 生成 | PDF-Lib + Fontkit + 本地中文字体 |
| 图标 | RemixIcon |
| 副屏富文本 | Pell |

**无 package.json / 无构建步骤 / 无外部 CDN**：所有依赖均已 vendored 到 `static/` 目录，源码即产物。

## 二、文件组织

```
D:\WorkBuddy\gift-book
├── index.html              # 主入口（约 303KB，内嵌全部业务代码）
├── guest-screen.html       # 宾客副屏入口
├── README.md               # 使用说明
├── License                 # 许可证
└── static/
    ├── tailwindcss.js      # Tailwind CSS 运行时
    ├── xlsx.full.min.js    # Excel 导出库
    ├── crypto-js.min.js    # 加密库
    ├── gridjs.umd.js       # 表格组件
    ├── pdf-lib.min.js      # PDF 生成
    ├── fontkit.umd.min.js  # 字体子集化/嵌入
    ├── GiftListPDFGenerator.js  # 业务级 PDF 渲染器（基于 pdf-lib）
    ├── pell.min.js / pell.min.css  # 副屏富文本编辑器
    ├── remixicon.css / .woff / .woff2  # 图标字体
    ├── mermaid.min.css     # 样式（未实际使用 Mermaid JS）
    ├── bg.jpg / bg.webp / cover1.jpg / cover2.jpg / addme.jpg / support.jpg  # 图片资源
    ├── MaShanZheng-Regular.ttf         # 姓名书法字体（PDF）
    ├── NotoSansSCMedium-mini.ttf       # 正文字体（PDF）
    └── SourceHanSerifCN-Heavy.ttf      # 标题/贺礼字体（PDF）
```

> 注：`static/1` 为一个 2 字节的未引用文件，疑似误生成，可忽略。

## 三、核心模块与职责

业务代码集中在 `index.html` 的 `<script type="module">` 中（已提取至 `.workbuddy/app.js` 方便阅读）。

### 3.1 基础设施层

| 类/对象 | 职责 |
|---------|------|
| `CONFIG` | 全局常量：每页 12 条、封面图 2MB 限制、打印分批阈值 1008 条、宾客等级数组、密码缓存 5 分钟等 |
| `DEFAULT_PRINT_OPTIONS` | 默认打印配置（封面/尾页/附录/汇总/作废可见性、PDF 引擎） |
| `Utils` | 通用工具：金额格式化、中文大写、文件转 Base64、日期时间、移动端检测 |
| `CryptoService` | AES 加密/解密 + SHA-256 哈希 |
| `DBManager` | IndexedDB 封装：打开数据库、升级 schema、增删改查、复合索引排序查询 |

**IndexedDB Schema（版本 1）**：
- `events`：事项信息（id 自增）
- `gifts`：礼金记录（id 自增），索引包括：
  - `eventId`
  - `byEventAndLevel`：复合索引 `[eventId, guestLevelWeight, levelUpdateTime, id]`，用于按等级排序

### 3.2 业务数据层

| 类 | 职责 |
|----|------|
| `GiftRepository` | 对 `DBManager` 的礼簿业务封装：`events`/`gifts` 的 CRUD |
| `SessionManager` | sessionStorage 会话持久化，保存当前事项与加密后的密码 |
| `PasswordCache` | localStorage 缓存管理员密码，5 分钟有效期 |

### 3.3 UI 层

| 类 | 职责 |
|----|------|
| `UIManager` | DOM 元素缓存、屏幕切换、模态框、通知 Toast、主题切换 |
| `GuestScreenService` | 宾客副屏的打开、BroadcastChannel/Storage 同步、隐私脱敏 |

### 3.4 核心功能层

| 类 | 职责 |
|----|------|
| `GiftManager` | 礼金记录的渲染、翻页、懒加载解密、缓存、修改/作废/纠错、历史快照 |
| `ExportService` | Excel 导出、PDF 打印（浏览器原生 + pdf-lib 双引擎）、备份/恢复 |
| `StatsService` | 统计弹窗：按类型/金额段汇总、最近记录、Top N 等 |
| `GiftBookApp` | **主控制器**：初始化、事件绑定、表单处理、密码验证、会话管理、全局状态 |
| `ImageCache` | IndexedDB 图片缓存（用于封面、收款码等） |

### 3.5 PDF 引擎

`static/GiftListPDFGenerator.js` 中定义 `GiftRegistryPDF` 类：
- 基于 `pdf-lib` 与 `fontkit`
- A4 横向布局
- 支持封面、内页、附录、尾页
- 可自定义字体、颜色、间距、背景图

## 四、关键数据流

### 4.1 创建事项

```
用户填写表单 → 二次确认密码 → GiftBookApp.handleCreateEvent()
    → CryptoService.hash(password) 生成 passwordHash
    → GiftRepository.createEvent(event) → IndexedDB events 表
    → startSession() 加载界面
```

### 4.2 录入礼金

```
用户提交 → handleAddGift() 校验 → showGiftConfirmationModal() 确认
    → saveGift() 加密并写入 gifts 表
    → 内存 gifts 数组追加新记录
    → GiftManager.renderSingleGift() 增量更新 DOM
    → GuestScreenService.syncToGuestScreen() 同步副屏
    → 语音播报（可选）
```

### 4.3 打开已有事项

```
选择事项 → handleUnlockEvent() → requestAdminPassword() 验证
    → startSession() → giftManager.loadGiftsForCurrentEvent()
        → DBManager.getAllByCompoundIndex() 按等级排序读取
        → 仅解密当前页数据（懒加载）
        → 后台 requestIdleCallback 分批解密剩余数据
```

### 4.4 修改/作废记录

```
点击单元格 → showGiftDetails() → performUpdate() / abolishGift()
    → 管理员密码验证
    → 生成 historyEntry 快照（changeLog + snapshot）
    → CryptoService.encrypt(updatedData, currentPassword)
    → GiftRepository.updateGift() → IndexedDB
    → 增量更新 DOM + 统计缓存 + 副屏同步
```

## 五、状态与缓存设计

`GiftBookApp` 维护以下关键状态：

```js
{
  currentEvent,        // 当前事项对象
  currentPassword,     // 当前会话明文密码
  gifts: [],           // 当前事项所有礼金记录（部分已解密）
  currentPage,         // 当前页码
  totalAmountCache,    // 总金额缓存
  totalGiversCache,    // 总人数缓存
  cachedStats,         // 统计详情缓存
  statsAreDirty,       // 统计是否需要重新计算
  allGiftsDecrypted,   // 是否全部解密完成
}
```

**性能优化点**：
- 礼金数据默认 AES 加密存储，只有当前页需要的数据才即时解密
- 后台通过 `requestIdleCallback` 分批解密，避免阻塞主线程
- 新增/修改记录时增量更新缓存，避免全量重算
- `renderSingleGift()` 仅更新单个单元格，不重建整个表格

## 六、安全与权限模型

| 场景 | 机制 |
|------|------|
| 数据存储 | `encryptedData` 字段为 AES 加密后的 JSON |
| 密码验证 | 事项创建时存储 `passwordHash = SHA-256(password)` |
| 敏感操作 | 修改姓名/金额、作废、超时补录等强制要求管理员密码 |
| 密码缓存 | 成功验证后缓存到 localStorage，5 分钟内免重复输入 |
| 会话恢复 | sessionStorage 保存事项与加密后的密码 |

## 七、主题与样式

- 内置两套 CSS 主题：`theme-festive`（喜庆红）、`theme-solemn`（肃穆灰）
- 通过 CSS 变量 `--primary-color` 等统一控制
- 事项创建时持久化 `theme`，`startSession()` 时应用

## 八、交互与快捷键

- `Enter`：在礼金录入区快速提交
- `Ctrl + P`：触发打印/PDF
- `←` / `→`：翻页
- `F11`：全屏（副屏提示）
- 点击姓名单元格：查看详情/修改
- 点击金额单元格：语音播报

## 九、后续开发建议

1. **代码组织**：当前全部业务代码内嵌于 `index.html`（5830 行），建议拆分为独立 ES 模块，便于维护与单元测试。
2. **构建工具**：可引入 Vite 等轻量构建工具，支持代码拆分、热更新、TypeScript 迁移。
3. **测试**：目前没有自动化测试，核心类（`Utils`、`CryptoService`、`GiftManager`）适合优先补充单元测试。
4. **国际化**：当前为纯中文，如有多语言需求，文本需抽离为字典。
5. **数据迁移**：IndexedDB schema 升级逻辑较简单，后续新增字段需注意旧数据兼容。
6. **副屏通信**：当前依赖 `BroadcastChannel` / `localStorage` 事件，跨标签页/窗口有效，但不支持跨设备。

## 十、验证结论

- ✅ 仓库已成功克隆到 `D:\WorkBuddy\gift-book`
- ✅ 所有依赖均为本地 vendored，无需包管理器安装
- ✅ 本地静态服务器可正常启动，所有资源路由返回 200
- ✅ 主应用可正常初始化、创建事项、录入礼金、显示统计
- ✅ 宾客副屏可正常加载
- ✅ 控制台无报错（仅有 Tailwind 本地构建提示）

项目已具备后续功能开发的基础条件。
